/**
 * GymBrat product focus: self-serve athlete (B2C). Coach–athlete CRM and multi-client
 * trainer workflows are intentionally out of scope until explicitly prioritized.
 */
export const PRODUCT_FOCUS = "b2c" as const;

export type ProductFocus = typeof PRODUCT_FOCUS;
